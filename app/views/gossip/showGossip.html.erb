<main class="container mt-5">
  <div class="row">
    <div class="col">
      <h1 class="text-center">Gossip n°<%= @gossip.id %></h1>
      <p class="text-center mt-3">Retrouvez sur cette page le détail du gossip n°<%= @gossip.id %></p>
      <p><%= @gossip.user.first_name %></p>
      <p><%= @gossip.title %></p>
      <p><%= @gossip.content %></p>
      <p><%= @gossip.created_at %></p>
      <a href="/user/<%= @gossip.user_id %>">Voir le profil de l'utilisateur</a>
    </div>
  </div>
</div>